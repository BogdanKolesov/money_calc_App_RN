import React from 'react';
import { View, StyleSheet } from 'react-native';

const Calc = () => {
    return (
        <View>

        </View>
    );
}

const styles = StyleSheet.create({})

export default Calc;
